import type { NextPage } from "next";
import CreateStrategyPage from "@/templates/CreateStrategyPage";

const CreateStrategy: NextPage = () => {
    return <CreateStrategyPage />;
};

export default CreateStrategy;
